# Peptide Optimization 环境配置

# 服务端口配置
PORT=8001

# 数据库配置 (PostgreSQL)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=secret
DB_NAME=mydatabase

# SeaweedFS 存储配置
SEAWEED_FILER_ENDPOINT=http://localhost:8888
# 注意：必须与 AstraMolecula 使用相同的 bucket
SEAWEED_BUCKET=astramolecula

# 任务处理配置
POLL_INTERVAL=30

# 日志配置
LOG_LEVEL=INFO
# LOG_DIR=/var/log/peptide_opt
# LOG_FILE=/var/log/peptide_opt/peptide_opt.log
